<!DOCTYPE html>
<html>
<head>
  <title>de olho no posto</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
 <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">De olho no Posto</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li <%= (request.original_fullpath == '/home' || request.original_fullpath == '/' || request.original_fullpath == '/home/index')? 'class=active' : ''%>><%= link_to 'Home', home_path %></a></li>
            <li <%= (request.original_fullpath == '/postos')? 'class=active' : ''%>><%= link_to "Postos", postos_path %></li>
            <li><a href="#contact">Contact</a></li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
 </nav>

    <div class="container">
      <div class="starter-template">
		<div style="text-align: left;">
		  <%= yield %>
		</div>
	  </div>
	</div>
</body>
</html>
